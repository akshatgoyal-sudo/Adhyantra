import { useEffect,useRef,type ReactNode } from "react";
import { Button } from "../ui";
import styles from "./AdminExperience.module.css";
export function AdminConfirmDialog({title,description,confirmLabel,busy=false,error,onCancel,onConfirm,children}:{title:string;description:string;confirmLabel:string;busy?:boolean;error?:string|null;onCancel:()=>void;onConfirm:()=>void;children?:ReactNode}){const cancel=useRef<HTMLButtonElement>(null);const dialog=useRef<HTMLDivElement>(null);const returnFocus=useRef<HTMLElement|null>(null);useEffect(()=>{returnFocus.current=document.activeElement instanceof HTMLElement?document.activeElement:null;cancel.current?.focus();const key=(e:KeyboardEvent)=>{if(e.key==="Escape"&&!busy)onCancel();if(e.key==="Tab"&&dialog.current){const items=[...dialog.current.querySelectorAll<HTMLElement>('button,[href],input,select,textarea')].filter(x=>!x.hasAttribute("disabled"));if(!items.length)return;const first=items[0],last=items[items.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}};document.addEventListener("keydown",key);return()=>{document.removeEventListener("keydown",key);returnFocus.current?.focus()}},[busy,onCancel]);return <div className={styles.dialogBackdrop} onMouseDown={e=>{if(e.target===e.currentTarget&&!busy)onCancel()}}><div ref={dialog} className={styles.dialog} role="dialog" aria-modal="true" aria-labelledby="admin-confirm-title" aria-describedby="admin-confirm-description"><h2 id="admin-confirm-title">{title}</h2><p id="admin-confirm-description" className={styles.muted}>{description}</p>{children}{error?<p role="alert">{error}</p>:null}<div className={styles.actions}><Button ref={cancel} variant="secondary" disabled={busy} onClick={onCancel}>Cancel</Button><Button variant="danger" loading={busy} onClick={onConfirm}>{confirmLabel}</Button></div></div></div>}
